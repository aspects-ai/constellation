/**
 * ConstellationFS MCP Server CLI
 *
 * This is a thin wrapper that imports the compiled MCP server.
 * The actual implementation is in src/mcp/server.ts (compiled to dist/mcp/server.js)
 */

import '../dist/mcp/server.js'
